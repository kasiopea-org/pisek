[tool.poetry]
name = "pisek"
version = "0.1.0"
description = "Nástroj na přípravu úloh do programátorských soutěží"
homepage = "https://github.com/kasiopea-org/pisek"
authors = []

[tool.poetry.scripts]
pisek = "pisek.__main__:main"

[tool.poetry.dependencies]
python = "^3.6"
tqdm = "^4.50.0"

[tool.poetry.dev-dependencies]
pytest = "^5.2"

[build-system]
requires = ["poetry-core>=1.0.8"]
build-backend = "poetry.core.masonry.api"
