#include <iostream>
using namespace std;

int main() {
    int t;
    cin >> t;
    while (t--) {
        // overflow
        int a, b;
        cin >> a >> b;
        cout << (a + b) << endl;
    }
}
